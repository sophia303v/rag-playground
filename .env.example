# Get your API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here

# Embedding backend: "sentence-transformers" (default) | "tfidf" | "gemini"
# EMBEDDING_BACKEND=sentence-transformers

# Generation backend: "gemini" (default) or "ollama" (local)
# GENERATION_BACKEND=gemini

# Vision backend: "gemini" (default) or "ollama" (local, uses llava)
# VISION_BACKEND=gemini

# Ollama settings (only needed when using ollama backends)
# OLLAMA_MODEL=llama3.2:3b
# OLLAMA_VISION_MODEL=llava:7b
# OLLAMA_BASE_URL=http://localhost:11434
